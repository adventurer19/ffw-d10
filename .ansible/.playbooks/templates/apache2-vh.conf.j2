<VirtualHost *:{{ http_port }}>
  ServerAdmin webmaster@localhost
  ServerName {{ http_host }}
  ServerAlias www.{{ http_host }}
  ErrorLog {{ error_log_dir }}/error.log
  CustomLog {{ custom_log_dir }}/access.log combined
  DocumentRoot "/var/www/{{ http_host }}/web"
</VirtualHost>
